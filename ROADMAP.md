# DevSupport Agent · 16 周路线

这份路线把“学习了什么”转换成“能用什么证据证明”。计划默认每周约 10 小时，AI 负责实现加速，我负责需求、架构、审查、验证和最终决策。

## 总体里程碑

| 阶段 | 周次 | 可交付成果 | 版本 |
|---|---:|---|---|
| Backend Foundation | 01–04 | 可测试、可迁移、可由 Web UI 调用的 Issue API | `v0.1` |
| Agent Core | 05–10 | 双运行引擎、会话记忆、工具调用与带引用 RAG | `v0.2` |
| Production Engineering | 11–15 | 审批状态机、MCP、架构 A/B、评测安全与部署演练 | `v0.3` |
| Portfolio | 16 | 可复现演示、工程证据和求职材料 | `v1.0` |

## 每周完成定义

除每周的专项验收外，每周还应满足：

- [ ] 任务开始前写清目标、约束、非目标和验收标准
- [ ] 代码通过当周适用的测试、lint 与类型检查
- [ ] 至少验证一个失败、边界或攻击场景
- [ ] 能不看代码说明关键请求的完整调用链
- [ ] 记录 AI 完成、我审查和我亲手修改的内容
- [ ] 更新周记、相关 ADR 与 `docs/ai-debt.md`
- [ ] `main` 保持可运行，不提交密钥或真实业务数据

---

## Phase 1 · Backend Foundation

### Week 01 · Python、Pydantic 与本地应用分层

**目标：** 建立最小但规范的 Python 工程，用 JSON 存储和 CLI 跑通 Issue 核心用例。

- [x] 初始化 `backend/`，配置 Python、依赖管理、lint、类型检查和 pytest
- [x] 使用 Pydantic 定义 `Issue`、优先级、状态与输入输出模型
- [ ] 划分 Service 与 Repository 边界，CLI 不直接操作存储
- [ ] 实现 JSON Repository，并明确序列化、文件不存在和损坏文件的行为
- [ ] 实现创建、查询和列表的 CLI 命令
- [ ] 为正常输入、空标题、非法优先级、损坏 JSON 和不存在记录编写测试
- [ ] 建立首个 CI 工作流，自动运行 lint、类型检查和 pytest
- [ ] 画出 `CLI → Service → Repository → JSON` 调用链并能口头解释

**阶段证据：** 可运行 CLI、测试结果、调用链图、Week 01 复盘、首个小步 PR。

### Week 02 · FastAPI 与稳定 API 契约

**目标：** 将领域能力暴露为可测试的 REST API，并统一成功与失败语义。

- [ ] 建立 FastAPI 应用工厂、路由和依赖注入
- [ ] 使用 Pydantic 定义请求、响应和错误 Schema
- [ ] 实现 Issue 创建、查询、列表和更新接口
- [ ] 明确 `201/200/404/409/422/500` 的使用场景
- [ ] 统一异常映射，响应中不泄露堆栈和内部实现
- [ ] 使用 API 测试验证契约、边界值和错误响应
- [ ] 生成并人工检查 OpenAPI 文档

**阶段证据：** OpenAPI、API 测试报告、接口规格、错误契约说明。

### Week 03 · SQLAlchemy、Alembic 与 SQLite

**目标：** 在 SQLite 上理解 ORM、Session 和数据库迁移，并保持上层业务接口稳定。

- [ ] 使用 SQLAlchemy Model 和 SQLite Repository 替换 JSON Repository
- [ ] 保持 Service 与 API 契约不因存储实现变化而改变
- [ ] 使用 Alembic 创建、升级并回滚第一版 Schema
- [ ] 理解并说明 Engine、Session、flush、commit 和 rollback 的职责
- [ ] 定义主键、唯一约束、时间字段与必要索引
- [ ] 为 Session 生命周期、事务回滚和唯一约束编写集成测试
- [ ] 比较 JSON 与关系数据库实现，记录 Repository 抽象的价值和代价

**阶段证据：** SQLite 集成测试、migration upgrade/downgrade 记录、数据模型图、事务 ADR。

### Week 04 · PostgreSQL、Docker、数据隔离与 React 最小联调

**目标：** 将数据库迁移到 PostgreSQL，补齐隔离与幂等，并完成最小前后端闭环。

- [ ] 将 SQLAlchemy 数据库配置从 SQLite 切换为 PostgreSQL
- [ ] 使用 Docker Compose 启动 API 与 PostgreSQL，并验证 Alembic 迁移
- [ ] 引入用户/租户归属，在后端查询层强制身份隔离
- [ ] 为创建操作增加幂等键，并用数据库唯一约束处理并发重复请求
- [ ] 验证事务回滚、索引和关键约束在 PostgreSQL 上的真实行为
- [ ] 初始化 React + TypeScript，完成 Issue 列表与创建表单的最小联调
- [ ] 正确处理 loading、empty、validation error 和 server error
- [ ] 配置 CORS、`.env.example` 和从零启动说明
- [ ] 完成端到端冒烟测试并发布 `v0.1-backend-foundation`

**阶段证据：** 可运行 Demo、截图或短视频、端到端测试、`v0.1` Release Notes。

---

## Phase 2 · Agent Core

### Week 05 · Model API 与 Structured Outputs

**目标：** 在独立实验中理解模型 API，再以可替换接口和结构化输出接入正式项目。

- [ ] 在 `labs/` 完成最小模型调用与错误处理实验
- [ ] 定义 `ModelClient` 边界，使业务测试不依赖真实模型
- [ ] 使用明确 Schema 接收 Structured Outputs，并在 Python 侧再次校验
- [ ] 为模型调用设置超时、取消和最大输出预算
- [ ] 使用 Fake Model 编写完全确定性的测试
- [ ] 记录提示词、模型版本和参数，避免不可追溯行为
- [ ] 对 Schema 不匹配、拒答、限流和超时建立失败测试

**阶段证据：** 模型适配器测试、Structured Outputs Schema、失败矩阵、模型边界 ADR。

### Week 06 · Function Calling 与类型化工具

**目标：** 理解 Function Calling 协议，并建立模型不能绕过的工具校验与执行边界。

- [ ] 跑通 `模型请求工具 → Python 执行 → 工具结果回传模型` 的完整链路
- [ ] 定义工具名称、说明、输入 Schema、输出 Schema 和错误类型
- [ ] 实现 Issue 查询等只读工具，并通过 Service 访问数据
- [ ] 在执行前校验工具是否存在、参数是否合法、调用者是否有权限
- [ ] 对外部调用设置超时，并区分可重试与不可重试错误
- [ ] 对工具结果进行大小限制、序列化检查和敏感字段过滤
- [ ] 测试未知工具、重复 call ID、非法参数、越权、超时和结果过大

**阶段证据：** Function Calling 时序图、工具契约、执行器与权限测试。

### Week 07 · 手写 Bounded Agent Loop

**目标：** 不依赖 Agent 框架，手工实现可理解、可测试且一定会停止的循环。

- [ ] 定义 run、step、message、tool call 和最终结果的运行结构
- [ ] 实现 `模型 → 工具 → 模型` 的多步循环
- [ ] 设置最大步数、工具次数、token、时间和重复调用限制
- [ ] 定义 completed、failed、cancelled、budget_exceeded 等终止结果
- [ ] 检测完全相同或无进展的工具调用，防止无限循环
- [ ] 使用脚本化 Fake Model 覆盖单步、多步、失败和预算耗尽
- [ ] 画出循环与退出路径，并能逐行解释核心推进逻辑

**阶段证据：** 循环图、确定性 Agent 测试、停止条件规格、手写 Loop ADR。

### Week 08 · Agents SDK 双引擎对照

**目标：** 在理解手写循环后接入 Agents SDK，用同一任务对照框架抽象与自研实现。

- [ ] 将手写 Loop 封装为统一 `AgentRuntime` 接口的一种实现
- [ ] 在另一实现中接入 Agents SDK，不把 SDK 类型泄漏到业务层
- [ ] 让两种引擎复用相同工具、权限规则和领域 Service
- [ ] 用配置切换运行引擎，不改变 API 请求与响应契约
- [ ] 使用同一组确定性场景验证两种引擎的行为
- [ ] 比较控制力、可观测性、错误语义、代码复杂度和升级成本
- [ ] 记录差异、适用场景与暂定默认方案，不预设 SDK 一定更优

**阶段证据：** 双引擎 Demo、契约测试、对照表、Agents SDK 采用 ADR。

### Week 09 · 会话、状态与记忆

**目标：** 建立隔离、可解释且受上下文预算约束的多轮会话能力。

- [ ] 定义 conversation、message、run 与 memory 的数据边界
- [ ] 持久化会话和消息，并在后端强制用户/会话隔离
- [ ] 区分原始消息、运行状态、短期上下文和提炼后的记忆
- [ ] 实现上下文选择、窗口裁剪或摘要策略，并记录选择依据
- [ ] 防止工具密钥、隐藏指令和其他用户内容进入长期记忆
- [ ] 支持创建、继续和读取会话，前端展示历史与当前状态
- [ ] 测试跨会话污染、超长上下文、重放和并发消息场景

**阶段证据：** 会话数据模型、上下文策略测试、隔离测试、记忆设计 ADR。

### Week 10 · RAG、可核验引用与拒答

**目标：** 用公开模拟资料建立端到端 RAG，让回答可追溯，并在证据不足时拒答。

- [ ] 建立文档清洗、分块、元数据、版本标识和可重复导入流程
- [ ] 保留来源、标题、章节和可定位片段，并实现检索工具
- [ ] 建立人工确认的问题、答案、相关文档与引用小数据集
- [ ] 生成答案时关联具体引用，前端可定位到对应来源片段
- [ ] 验证引用确实支持对应结论，而不只检查“存在链接”
- [ ] 对无相关结果、证据冲突或证据不足的问题明确拒答或表达不确定
- [ ] 测试检索注入、来源伪造、重复文档和恶意文档内容
- [ ] 发布 `v0.2-agent-core`，报告 RAG 基线与已知限制

**阶段证据：** RAG 数据流图、引用/拒答评测、错误案例集、`v0.2` Release Notes。

---

## Phase 3 · Production Engineering

### Week 11 · 审批、幂等与持久化状态机

**目标：** 将“模型建议操作”和“系统获准执行”分离，并让执行在重启后仍可安全恢复。

- [ ] 为 Agent run 定义 pending、running、waiting_approval、completed、failed 和 cancelled 状态
- [ ] 将 run、step、工具调用、审批请求和状态转换持久化到 PostgreSQL
- [ ] 为 Issue 创建/更新工具定义预览、待审批与正式写入阶段
- [ ] 要求明确用户确认，并验证审批人、资源权限和参数摘要
- [ ] 使用幂等键与数据库约束防止重试、双击或恢复造成重复写入
- [ ] 为状态推进增加并发保护，拒绝非法或重复状态转换
- [ ] 模拟审批过期、拒绝、进程中断和恢复，验证不会跳过审批或双写

**阶段证据：** 持久化状态图、重启恢复测试、审批 Demo、状态机/幂等 ADR。

### Week 12 · Python MCP Server

**目标：** 用 Python 实现最小 MCP Server，同时保持业务与权限规则只有一份。

- [ ] 在 `mcp_server/` 用 Python 实现最小 Resource、Prompt 或只读 Tool
- [ ] 复用 Application Service，不复制业务规则和数据库访问
- [ ] 为 MCP 输入、输出、错误、认证和超时定义类型化契约
- [ ] 明确本地与远程 MCP Server 的信任差异
- [ ] 限制可用工具、参数、结果大小和调用频率
- [ ] 使用真实 MCP 客户端完成连通性测试
- [ ] 为核心逻辑保留确定性测试，并验证 Server 不可绕过租户权限

**阶段证据：** Python MCP Demo、契约测试、信任边界说明、MCP 集成 ADR。

### Week 13 · 单 Agent / 多 Agent A/B

**目标：** 在同一任务集上比较单 Agent 与多 Agent，用数据决定是否值得增加复杂度。

- [ ] 保留单 Agent 作为可工作的基线方案
- [ ] 实现一个职责清晰、交接显式的最小多 Agent 候选方案
- [ ] 让两种方案使用相同工具、数据、模型配置和评测样本
- [ ] 测量任务成功率、工具选择、延迟、token、成本和失败类型
- [ ] 检查多 Agent 的上下文丢失、循环委派、权限扩大和错误归因问题
- [ ] 记录至少一次失败样本的完整运行对照
- [ ] 根据 A/B 结果选择默认架构，并写明何时重新评估

**阶段证据：** A/B 报告、可复现配置、代表性 Trace、单/多 Agent ADR。

### Week 14 · Eval、Tracing 与安全

**目标：** 用版本化评测、可追踪运行和攻击用例发现静默退化与越权风险。

- [ ] 设计覆盖成功、边界、拒答、审批、恢复和攻击的固定 eval cases
- [ ] 区分确定性规则评分、模型评分与人工复核
- [ ] 记录模型、Prompt、工具 Schema、数据集和运行参数版本
- [ ] 统一 request/run/step/tool correlation ID 并保存可定位 Trace
- [ ] 评测任务成功率、工具参数、引用支持、延迟和成本
- [ ] 覆盖 Prompt Injection、越权工具调用、恶意 MCP 输出和审批复用
- [ ] 验证日志、Trace 和评测样本不含密钥与敏感数据
- [ ] 在 PR CI 运行确定性评测，真实模型评测使用手动 workflow 和费用上限

**阶段证据：** 版本化评测报告、安全测试、脱敏 Trace、回归门槛和威胁模型。

### Week 15 · 部署与故障演练

**目标：** 部署可复现版本，并证明关键依赖失败时系统能够受控降级和恢复。

- [ ] 建立生产式镜像、配置校验、健康检查和数据库迁移流程
- [ ] 部署到一个可复现环境，验证前端、API、数据库和 Agent 主路径
- [ ] 为模型与外部工具设置超时、有限重试、退避和并发限制
- [ ] 演练模型超时、限流、工具失败、数据库断连和进程重启
- [ ] 验证 pending approval 与运行状态在重启后不会丢失或重复执行
- [ ] 建立备份恢复、密钥轮换、版本回滚和故障处置手册
- [ ] 记录恢复时间、数据一致性和用户可见行为，而不只记录“服务恢复”
- [ ] 发布 `v0.3-production-ready`，如实列出残余风险和已知限制

**阶段证据：** 部署地址或录屏、故障演练记录、恢复/回滚手册、`v0.3` Release Notes。

---

## Phase 4 · Portfolio

### Week 16 · 作品集、演示与 v1.0

**目标：** 把工程成果整理成陌生评审者可以验证的求职作品。

- [ ] 完成 README 的真实架构、功能、指标、快速开始和限制说明
- [ ] 制作 3–5 分钟演示，包含正常流程、审批和一个失败场景
- [ ] 清理仓库，确保无密钥、真实业务数据、失效链接和伪造指标
- [ ] 从零环境验证安装、迁移、启动、测试和演示步骤
- [ ] 整理关键 PR、ADR、评测和故障复盘作为能力证据
- [ ] 准备项目讲解：问题、取舍、调用链、失败处理和下一步
- [ ] 发布 `v1.0-portfolio`，附 Demo、评测结果与已知限制

**阶段证据：** 可复现仓库、演示视频、面试讲稿、`v1.0` Release Notes。

---

## 版本发布门槛

每个里程碑只有在以下条件全部满足时才能发布：

- [ ] 对应周次的专项验收已完成，未完成项有明确说明
- [ ] 主分支 CI 通过，部署/启动步骤由干净环境验证
- [ ] Release Notes 包含新增能力、测试证据和已知限制
- [ ] 指标能追溯到固定数据集与运行配置
- [ ] 不包含密钥、真实用户数据或未授权资料
- [ ] 至少演示一个失败场景及系统如何安全处理

## 进度记录约定

- 路线状态只在有代码、测试或文档证据后更新。
- 详细过程写入 `docs/weeks/week-XX.md`，本文件只保留目标与验收状态。
- 重要决策写入 `docs/adr/`；决策改变时新增 ADR，不重写历史。
- 实验放在 `labs/`，正式项目不得直接依赖实验代码。
- AI 生成但尚未理解的实现写入 `docs/ai-debt.md`，每周至少解决一项。
